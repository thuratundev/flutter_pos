import 'package:flutter/material.dart';

class ResTableView extends StatelessWidget {
  const ResTableView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('I am TableView'),
      ),
    );
  }
}